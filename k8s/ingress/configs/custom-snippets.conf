# Custom Nginx snippets
# Add custom nginx configuration that will be included in the server block

# Example: Custom location block
# location /custom-path {
#     return 200 "Custom response";
#     add_header Content-Type text/plain;
# }

# Example: Custom rate limiting zone
# limit_req_zone $binary_remote_addr zone=custom_limit:10m rate=10r/s;

# Example: Custom upstream keepalive
# upstream backend {
#     least_conn;
#     keepalive 32;
# }

# Example: Custom caching
# proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=my_cache:10m max_size=1g inactive=60m use_temp_path=off;

# Example: GeoIP configuration (if GeoIP module is enabled)
# geoip_country /usr/share/GeoIP/GeoIP.dat;
# geoip_city /usr/share/GeoIP/GeoLiteCity.dat;

# Add your custom nginx configuration here
